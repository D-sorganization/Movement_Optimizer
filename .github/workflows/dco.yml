name: DCO
on:
  pull_request:
    branches: [main]
jobs:
  dco:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: DCO Check
        uses: src-d/guide/dco@master
        with:
          setup_git: false
